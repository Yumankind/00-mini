{
  "name": "@00/agent-fs",
  "version": "0.0.1",
  "private": true,
  "description": "Infinite Agent: the agent folder as a filesystem — OPFS and in-memory adapters, the bundle (.00agent) writer and reader, git.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": { "@00/shared": "workspace:*", "isomorphic-git": "^1.27.0" },
  "devDependencies": {
    "@vitest/coverage-v8": "2.1.9",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
